{
  "name": "ringo-polygon",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "test": "graph test",
    "deploy": "graph deploy ringo-polygon",
    "publish": "graph publish",
    "start-block": "node scripts/find-start-block.mjs"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.97.1",
    "@graphprotocol/graph-ts": "0.38.0"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
